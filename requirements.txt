python-telegram-bot==21.6
aiohttp>=3.9
croniter>=2.0
playwright>=1.40
python-dotenv>=1.0  # used by app.py to load .env

# --- Optional adapters (install only if you want that platform) ---
# Discord:
#   pip install 'discord.py>=2.4'
# Slack:
#   pip install 'slack-bolt>=1.18'
